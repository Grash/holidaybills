  var trip = angular.module('holidaybills');
  trip.controller('NewTripCtrl',['$scope', '$location',  function($scope, $location) {
//      $scope.tripId = 0;
//      var unregister = $scope.$watch('tripId', function (newValue, oldValue) {
//          if(newValue != undefined && newValue != oldValue){
////              unregister();
//              $location.path("/trip/"+newValue);
//          }
//      }, true);
    }]);